# 장편 루트 관리

현재 구현된 프롤로그는 공통 단일 루트다. 이 폴더에 새 루트를 작성할 때는 루트 목적, 시작 장면 ID, 진입 조건, 배타 조건, 선택별 결과, 합류 장면 ID, 종료/후일담을 기록한다. 각 챕터의 실제 대사는 `scenario/chapters/` 하위 해당 루트에 둔다.

기획 문서가 생겼다고 자동으로 게임 분기가 추가되지는 않는다. 반영 요청 시 재생기와 저장 조건을 함께 구현하고 검증한다.
